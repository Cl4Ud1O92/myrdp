name: RDP-GoogleDrive-Fixed

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 360  # Limite massimo di 6 ore per i runner gratuiti

    steps:
      - name: Configure Core RDP Settings
        run: |
          # Abilita RDP e disabilita NLA per compatibilità
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 0 -Force
          
          # Regole Firewall
          netsh advfirewall firewall delete rule name="RDP-Tailscale"
          netsh advfirewall firewall add rule name="RDP-Tailscale" dir=in action=allow protocol=TCP localport=3389
          Restart-Service -Name TermService -Force

      - name: Create RDP User
        run: |
          # Password robusta per evitare l'errore InvalidPasswordException
          $password = "Password-Gdrive-2026!" 
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires -Description "RDP User"
          Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"
          echo "RDP_PASS=$password" >> $env:GITHUB_ENV

      - name: Install Tools (Tailscale, Rclone, WinFsp)
        run: |
          # Installazione dei componenti necessari per il Cloud Drive
          choco install tailscale rclone winfsp -y

      - name: Configure Rclone
        run: |
          # Configurazione per entrambi gli utenti (System e RDP)
          $configContent = @"
          ${{ secrets.RCLONE_CONFIG_CONTENT }}
          "@
          $paths = @("$env:USERPROFILE\.config\rclone\rclone.conf", "C:\Users\RDP\.config\rclone\rclone.conf")
          foreach ($path in $paths) {
              New-Item -ItemType Directory -Force -Path (Split-Path $path)
              Set-Content -Path $path -Value $configContent
          }

      - name: Establish Tailscale Connection
        run: |
          & "C:\Program Files\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-runner-win
          $tsIP = & "C:\Program Files\Tailscale\tailscale.exe" ip -4
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV

      - name: Mount Google Drive
        run: |
          # Avvio del mount in background
          Start-Process rclone -ArgumentList "mount", "cla:", "Z:", "--vfs-cache-mode", "full", "--vfs-cache-max-size", "2G", "--allow-other" -WindowStyle Hidden
          
          # Aspetta che il disco Z: sia pronto
          Start-Sleep -Seconds 20
          
          # Creazione di un collegamento sul Desktop per l'utente RDP
          $desktopPath = "C:\Users\RDP\Desktop\I-Miei-File-Cloud"
          New-Item -ItemType Junction -Path $desktopPath -Target "Z:\" -ErrorAction SilentlyContinue

      - name: Maintain Connection
        run: |
          Write-Host "`n==============================="
          Write-Host " ACCESSO RDP PRONTO"
          Write-Host " IP: $env:TAILSCALE_IP"
          Write-Host " Utente: RDP"
          Write-Host " Password: $env:RDP_PASS"
          Write-Host "==============================="
          Write-Host " NOTA: Cerca la cartella 'I-Miei-File-Cloud' sul Desktop"
          
          while ($true) {
              Write-Host "[$(Get-Date)] Runner in esecuzione... (Disco Z: attivo)"
              Start-Sleep -Seconds 300
          }
