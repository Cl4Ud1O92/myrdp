name: RDP-Gdrive-Fixed-Machine

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - name: Configure Core RDP Settings
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 0 -Force
          netsh advfirewall firewall delete rule name="RDP-Tailscale"
          netsh advfirewall firewall add rule name="RDP-Tailscale" dir=in action=allow protocol=TCP localport=3389
          Restart-Service -Name TermService -Force

      - name: Create RDP User
        run: |
          $password = "Password-Gdrive-2026!" 
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          echo "RDP_PASS=$password" >> $env:GITHUB_ENV

      - name: Install Tools
        run: choco install tailscale rclone winfsp -y

      - name: Configure Rclone (Safe Path)
        run: |
          $rcloneDir = "C:\rclone_data"
          if (!(Test-Path $rcloneDir)) { New-Item -ItemType Directory -Path $rcloneDir }
          $configContent = @"
          ${{ secrets.RCLONE_CONFIG_CONTENT }}
          "@
          Set-Content -Path (Join-Path $rcloneDir "rclone.conf") -Value $configContent
          icacls $rcloneDir /grant "Everyone:(OI)(CI)F" /T

      - name: Establish Tailscale Connection (Identity Fix)
        run: |
          # Forziamo il nome 'gh-runner-win' per usare sempre lo stesso slot nel tuo account
          & "C:\Program Files\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-runner-win --accept-routes --reset
          $tsIP = & "C:\Program Files\Tailscale\tailscale.exe" ip -4
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV

      - name: Setup Cloud Mounter on Desktop
        run: |
          $userProfiles = Get-ChildItem "C:\Users"
          foreach ($profile in $userProfiles) {
              $desktopPath = Join-Path $profile.FullName "Desktop"
              if (Test-Path $desktopPath) {
                  $batContent = @"
          @echo off
          echo AVVIO GOOGLE DRIVE (Z:)...
          taskkill /f /im rclone.exe >nul 2>&1
          start /b "" "rclone" mount cla: Z: --vfs-cache-mode full --config "C:\rclone_data\rclone.conf"
          timeout /t 15
          if exist Z:\ ( explorer Z: ) else ( echo ERRORE: Disco Z non apparso. )
          exit
          "@
                  Set-Content -Path (Join-Path $desktopPath "ATTIVA-GOOGLE-DRIVE.bat") -Value $batContent
              }
          }

      - name: Maintain Connection
        run: |
          Write-Host "IP: $env:TAILSCALE_IP (Dovrebbe essere sempre lo stesso)"
          while ($true) { Start-Sleep -Seconds 300 }
